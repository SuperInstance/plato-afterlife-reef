[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-afterlife-reef"
version = "0.1.0"
description = "Afterlife reef — ghost tile long-term storage"
license = {text = "MIT"}
requires-python = ">=3.8"
